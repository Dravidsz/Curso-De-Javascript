/*
 -son variables que estan dentro de una funcion y no estan disponible en todo el codigo
*/
var obtnerNombre = (nombre) =>{
    var numero= nombre.length;

    console.log(`El nombre es ${nombre} y tiene ${numero} de letras`);
};


obtnerNombre('Aispurua');